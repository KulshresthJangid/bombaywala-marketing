# Contact form email delivery (api/contact.ts) — required for the contact
# form to actually send messages. Get a key at https://resend.com (free tier).
# Set this in .env.local for local `vercel dev` testing, and in the Vercel
# project's Environment Variables for production/preview deploys.
RESEND_API_KEY=
